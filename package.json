{
  "name": "@personal-ontology/finance-panel",
  "version": "0.0.1",
  "description": "Personal finance panel for personal-ontology — pulls bank balances and transactions via Enable Banking (PSD2 AISP). Single-user, self-hosted, bring-your-own-credentials.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/cli.ts"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsonwebtoken": "^9.0.7",
    "typescript": "^5.6.0"
  }
}
